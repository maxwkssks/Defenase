using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GuardianStatus : MonoBehaviour
{
    public int UpgradeCost { get; internal set; }
    public int MaxTargetCount { get; internal set; }
    public int Damage { get; internal set; }
    public float AttackCycleTime { get; internal set; }
    public float AttackRadius { get; internal set; }
    public Color Color { get; internal set; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
